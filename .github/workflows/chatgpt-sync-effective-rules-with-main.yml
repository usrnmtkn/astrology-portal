name: Sync effective rules runtime with current main

on:
  push:
    branches:
      - chatgpt/effective-writing-rules-runtime-current-main-v2
    paths:
      - .github/workflows/chatgpt-sync-effective-rules-with-main.yml

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-24.04
    steps:
      - name: Check out runtime branch
        uses: actions/checkout@v6
        with:
          ref: chatgpt/effective-writing-rules-runtime-current-main-v2
          fetch-depth: 0
      - name: Merge current main
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git fetch origin main
          git merge --no-edit origin/main
      - name: Remove temporary workflow and push
        run: |
          rm .github/workflows/chatgpt-sync-effective-rules-with-main.yml
          git add .github/workflows/chatgpt-sync-effective-rules-with-main.yml
          git commit --amend --no-edit
          git push origin HEAD:chatgpt/effective-writing-rules-runtime-current-main-v2
