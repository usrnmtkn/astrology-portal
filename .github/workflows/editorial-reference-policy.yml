name: Editorial reference policy
on:
  pull_request:
  push:
    branches: [main]
permissions:
  contents: read
concurrency:
  group: editorial-reference-policy-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true
jobs:
  reference-policy:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: node scripts/test-editorial-reference-policy.mjs
