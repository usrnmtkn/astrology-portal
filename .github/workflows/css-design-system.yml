name: CSS design system

on:
  pull_request:
    paths:
      - "apps/web/src/styles/**"
      - "apps/web/src/styles.css"
      - "apps/admin/src/**/*.css"
      - ".github/workflows/css-design-system.yml"
  push:
    branches:
      - main
    paths:
      - "apps/web/src/styles/**"
      - "apps/web/src/styles.css"
      - "apps/admin/src/**/*.css"
      - ".github/workflows/css-design-system.yml"

concurrency:
  group: css-design-system-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  css-design-system:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Run shared CSS audits
        run: npm run qa:css-audit
      - name: Run Content Studio architecture audit
        run: node scripts/run-studio-css-architecture-audit.mjs
      - name: Verify approved Studio layer tokens
        run: node scripts/test-studio-layer-tokens.mjs
