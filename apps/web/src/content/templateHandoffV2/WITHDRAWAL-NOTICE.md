# Earlier ZIPs withdrawn

The following archive lineage is withdrawn for implementation purposes:

- `tldr-astro-handoff-FINAL-SOURCE-GROUNDED-TEMPLATES.zip`
- any earlier fallback/template/content ZIP derived from it

Those archives contained useful evidence, but they also contained competing transit templates, ambiguous READY classifications, and insufficient render-component rules. Use this v2 archive only.

Do not delete already accepted repository work solely because it originated from an older package. Evaluate it against this archive's contracts and retain correct exact-pair selection, immutable fact separation, provenance, and `SOURCE_GAP` behavior.

