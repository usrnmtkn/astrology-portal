# TLDR Astro template handoff v2

This archive replaces every earlier template/content handoff ZIP. Do not merge its instructions with an older prompt.

## Goal

Finish the source-grounded fallback/template and admin-preview update so each reader surface uses its own narrative model, exact calculated facts, reviewed source clauses, and deterministic render contract.

This archive does **not** authorize changes to the accepted calculation/API checkpoint, Dashboard information architecture, unrelated styling, courses, audio, or navigation.

## Required reading order

1. `CODEX-IMPLEMENTATION-PROMPT.md`
2. `AUTHORITIES.md`
3. `INFORMATION-TREE.md`
4. `TLDR-ASTRO-MUSTACHE-MADLIBS-v2.2.md`
5. `contracts/SURFACE-RESOLUTION-MATRIX.json`
6. `contracts/EXECUTABLE-TEMPLATE-CONTRACT.json`
7. `contracts/RENDER-FIELD-CONTRACT.md`
8. `contracts/SOURCE-TIER-CONTRACT.md`
9. `contracts/VOICE-AND-NARRATIVE-CONTRACT.md`
10. `contracts/SECT-ELIGIBILITY-CONTRACT.md`
11. `ACCEPTANCE-GATES.md`

Run the package checks before touching the repository:

```bash
node tests/verify-package-contract.mjs
node tests/verify-render-fixture-shape.mjs
```

## Non-negotiable distinction

The files under `sources/` and `references/` are evidence. They are not reader-ready copy. A source key existing does not make a record READY.

The implementation must preserve immutable astrology facts, exact-source provenance, optional narrative slots, surface-specific rendering, `SOURCE_GAP` behavior, and final rendered-output tests.

`TLDR-ASTRO-MUSTACHE-MADLIBS-v2.2.md` is the literal interpolation authority. The JSON contract describes fields and eligibility; the Mustache file defines the actual prose layouts and optional blocks. Do not replace its `{{slots}}` with keyword arrays or treat its filled fixtures as universal copy.
