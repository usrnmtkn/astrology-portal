# Codex handoff: transits to natal + synastry (v1)

## Split of responsibilities

The package supplies COPY. The app engine supplies SELECTION. Codex implements selection per `TLDR-Daily-Transit-Baseline-Spec.md`; the package renders whatever the engine picks.

### Selection logic (engine side, from the owner's spec)

1. **Headline driver: the transiting Moon.** Its sign, whole-sign house in the reader's chart, and applying aspects pick the day's headline card. Never headline with a slow transit; the Moon is why dailies change daily.
2. **Areas of your life:** one section per qualifying non-Moon transit, ordered by orb tightness. Orb gates: outers + Saturn qualify at <= 3 degrees, inners at <= 5 degrees. Wider transits do not appear that day.
3. **Headliner days:** any Saturn-through-Pluto transit at orb <= 1.0 degree, any station on a natal point, or any return upgrades the day and earns the bespoke deep section. Never generate a bespoke deep section for a sub-threshold transit.
4. **Beat dedupe:** some authored cards share a rhetorical beat (e.g. the North Node "stop calling it coincidence" card and the Mercury retrograde "none of it is random" card). Do not surface two same-beat cards on one day; the affected cards carry the pairing in their `editorial_notes` field (moved there from the bodies in the 2026-07-22 cleanup — `editorial_notes` is authoring/engine metadata and NEVER renders).
5. Every daily card needs its derivation record (driving transit + orb + application, house of contact, rulership condition, source grounding) per spec section 8.

### Rendering (package side)

- `renderTransitAspect({ transiting, natal, aspect, variant, window, voice })` — pass `voice: friendName` on friend charts: renders authored friend-voice fallback only (the reader-voice authored library never appears in friend view; variants are reader-only). Same for `renderTransitHouse`. Friend retro/return seasons: use `renderTransitLabel` one-liners, not full cards.
  - Authored cards render first (mirror rule, heavy-aware conjunction sharing); fallback template otherwise; SOURCE_GAP only for out-of-package bodies.
  - `window` override: pass an exact phrase ("For the next ten days") when the engine knows the real window; otherwise speed-based defaults apply (Moon = "Today", Sun/Mercury/Venus = "This week", Mars = "For the next couple of weeks", Jupiter = "This month", slow bodies = "For the next few months").
  - Covers all 13 transiting bodies x 16 natal targets (13 bodies + 4 angles) x 5 aspects: verified 1,040/1,040 render.
- **Composer register on the standalone fallback (2026-07-28a):** `renderTransitAspect`'s non-authored path now renders opener (real aspect verb: squaring/opposing/trining/sextiling/sitting right on) + wants-pair + pair scenes, replacing the 'In plain terms' scaffold, for BOTH voices. Wants-clauses cover all 13 transiting bodies x 12 signs plus sign-less fallbacks — keep passing `sign` for the sign-aware line. Authored Blueprint cards still render first and are untouched. Legacy template survives only where composer rows are missing.
- Fallback variant rotation: the 26 transit effect lines each have 2 variants (`.../variant-2`, `.../variant-3`). Pass `variant: 2` or `variant: 3` to `renderTransitAspect` to rotate wording for repeat viewers (suggested: stable per user+transit, e.g. `(userId + transitId) % 3`, so a reader sees consistent copy within one transit but fresh copy across transits). No variant param = base line.
- `renderTransitHouse({ planet, house, sign, window, voice, variant })` — **two-layer authored lane (2026-07-25):** when `sign` is passed and the planet has authored layers (Mars: all 144 house-sign combos, dual voice), the card renders `authored/transit-house-intro/{planet}/{house}` + `authored/transit-house-sign/{planet}/{house}/{sign}` stacked (result `parts` = [intro, synthesis]; `templateKey: "authored/transit-house-layered"`; `window` returned as a field for chrome — the authored bodies carry no dates, so the app shows the real window in the card header). ALWAYS pass `sign` — without it the card falls back to the legacy one-liner and the sign layer is lost. **Aspect events layer (2026-07-25c):** pass `events: [{ natal, aspect, window? }]` (the same orb-gated qualifying aspects the daily already computes, ordered by orb tightness) and each event renders as an extra part beneath the synthesis via the **sky-register composer (2026-07-27a)**: trimmed frame (window folds in as ` until {date}` when passed (owner ruling 2026-07-27: 'until', never 'through'; windows arriving with their own preposition pass through normalized)) + wants-pair sentence (`transit-house-event-wants/{planet}/{sign}` + `transit-house-event-natal/{natal}`) + pair scenes (`transit-house-event-scenes/{planet}/{natal}/{hard|soft}` owner-override first, else the pair effect line). No more label sentence or 'In plain terms' scaffold. Falls back to the legacy frame+aspect-body stitch only when composer rows are missing for a planet. A SOURCE_GAP on any single event is skipped silently, never blocking the house card. Layered lane only; the legacy path ignores `events`. `variant: 2` rotates to the Marie Satori register rows (houses 1–7; falls back to base where absent; same stable per user+transit rotation as other variant rows). Friend charts: pass `voice: friendName` as usual — the layered rows carry authored `body_they`, so friend view now gets the full card, not the fallback. Planets without authored layers (everything except Mars) render the legacy template unchanged; 156/156 legacy grid still verified.
- `renderSynastryAspect({ planetA, planetB, aspect, otherName })` — the CONNECTIONS page (owner naming: the between-two-charts surface is called "Connections" in the app, not "Bonds"). planetA is the reader's. Returns `headline` (astrology), `tag` (plain-English quality chip: Easy Understanding / Easy Opening / Productive Friction / Balancing Act / Amplified), `body`. Direction matters; do not swap A/B to reuse a render.
- `renderTransitRetro({ planet, sign, window })` — retrograde season card (checks `authored/transit-retro/{planet}` first). Covers the nine bodies that retrograde: Mercury through Pluto plus Chiron. Sun/Moon never retrograde and the nodes almost always are, so those are SOURCE_GAP by design. Default windows use typical retrograde lengths; always pass real dates ("Until August 11") when known.
- `renderTransitAspect` also accepts `isRetrograde: true` — the fallback render appends the repeat-pass line ("this contact usually repeats... revise, not redo"). Authored cards stay verbatim; show retro context via `renderTransitRetro` alongside if needed.
- `renderCompat` — unchanged; refuses reversed directional pair cards by design.
- `renderTransitLabel({ transiting, natal, aspect, window })` — one-line labels for the daily "behind this forecast" stack (Co-Star pattern): `{Planet} {transforming|challenging|boosting} {life area}` plus a duration string. Verb map: conjunction = transforming, hard = challenging, soft = boosting ("facilitating" is banned vocabulary). Pass `window` with the real ephemeris phrase ("Through Saturday", "Two weeks"); order the list soonest-ending first. Tapping an entry opens the full `renderTransitAspect` page.

- `renderBondTransit({ transiting, aspect, planetA, planetB, natalAspect, otherName, sign, variant, window })` — `variant: 2` or `3` rotates the effect line for repeat viewers (same rotation rule as natal transit cards: stable per user+transit). Friend-voice `renderTransitAspect`/`renderTransitHouse` calls now also accept `variant` (the variant rows carry they-voice bodies). — connection transits (Connections page; function name kept for API stability): a transiting planet activating a synastry contact between the reader and a friend. `planetA` is always the reader's planet, `planetB` the friend's, `natalAspect` the synastry aspect between them (colors the bond-quality line). Selection (engine): only contacts already surfaced on the pair's synastry page qualify; gate the transit at <= 1 degree to either endpoint of the contact (tighter than natal transits, because two charts are involved); pass the real `window`. Renders reader-perspective; produce the friend's mirrored card by calling again with roles swapped, never by editing output. Surfaces: friend profile ("between you two right now"), circle feed (reader-inclusive tier-2 story when active), notifications. 260 combos verified, parity 195/195.
- `renderLunationHoroscope({ kind, sign, risingSign, eventDate?, matchingNewMoon?, house?, weekly? })` — per-rising lunation horoscope in the owner-approved weekly shape: recognizable situation -> house frame -> required matching-New-Moon anchor for ordinary Full Moons -> compact reviewed sign core -> woven Full Moon counterpoint -> traditional ruler-house paragraph and retrograde overlay -> one present-tense weekly ending when `weekly: true`. The retained Uranus layer rows are non-serving. The full Sky sign section and the former manifestations / moment / Release / Higher Path closer stack do not render in this card. `kind` accepts new-moon/full-moon/eclipse-solar/eclipse-lunar. Ordinary `full-moon` calls must provide the engine-verified prior same-sign `matchingNewMoon`; eclipses keep their separate contract. House auto-derives whole-sign from `risingSign` + lunation sign; moving-body houses come from event-time facts.
- `renderLunationEventCard({ eventDate, ...blendFacts, blendFallbackEnabled? })` — You-page-only register resolver. It first reads the approved per-event Satori key `authored/satori-lunation/{YYYY-MM-DD}/{sign}-rising`. When no Satori card exists, the app serves the owner-approved exact book cell through the blend fallback. `VITE_ENABLE_LUNATION_BLEND_YOU_FALLBACK=false` remains an emergency rollback switch; the app defaults it on. Weekly continues to call `renderLunationHoroscope` directly.
- `renderDoDont({ planet, sign, house, transiting, weakPlanet?, weakSign? })` — the Do/Don't engine per `TLDR-Remedial-DoDont-Spec.md`. The ENGINE picks the transited natal planet (tightest qualifying transit); the renderer assembles Do (sign seed + house seed + transiting counterweight) and Don't (placement shadow + transit friction + aggravated-aspect partner's shadow via weakPlanet/weakSign). Returns { do: string[3], dont: string[3] }. Seed coverage: Moon/Venus/Mars/Mercury/Saturn x 12 signs; other natal planets SOURCE_GAP by design (spec priority set). 780 assemblies verified, spec worked-examples asserted in tests.
- `renderSkyAspectCard({ a, b, aspect, aSign, bSign, dateLine })` — the Gifts/Lessons cards on sky placement pages. Collective voice, slower-planet-acts logic, 390/390 combos verified. Sky pages must never serve `renderTransitAspect` output (that is natal-chart copy for the You page).
- `renderCircleStory({ trigger, names, includesReader, members, ... })` — Friends Circle feed group cards; full API and product logic in `FRIENDS-CIRCLE-FEED-SPEC.md`. Also exports `formatCircleNames`.
- `renderPairDaily({ reader: { handle, clauseKey }, friend: { handle, displayName?, clauseKey }, shared: { kind, family?, element?, bondClauseKey?, transiting? }, variant })` — "Today between you two" paragraph assembled from the owner-approved `pair-daily-frames-v1.json` and `pair-daily-clauses-v1.json` rows. The engine reuses the Daily At-a-Glance driver for each chart and the first already-ranked bond card, falling back to the current Moon only when it aspects both charts inside the daily gate. Variant selection derives from each approved family’s actual row count. Reader openers use `@handle` plus you-voice; a missing reader handle forces handle-free opener variant 3. Friend clauses use `body_they`, with friend reference fallback from `@handle` to display name to "your friend." The hard close serves only for Saturn or Mercury bond transits. Missing frames, clauses, drivers, or unresolved slots still fail closed through `SourceGapError`. This renderer never accepts or emits transit windows: "today" is the only allowed window word.
- `renderSkyLunation` upgrades: accepts `kind: "eclipse-solar" | "eclipse-lunar"` (eclipse opener/close canon, NO intention/ritual sections on eclipses), `northSign`/`southSign` (nodal axis paragraph), and `variant` (e.g. `"year-end"` for the NYE Capricorn New Moon). The per-sign section now renders the owner's authored cards first (all 12 NM + 12 FM ingested, with Set-your-intention / Ritual / closing-question paragraphs appended on regular lunations). Headline becomes "Solar/Lunar Eclipse in {Sign}" for eclipse kinds. See `OWNER-LUNATION-TEMPLATE-LIBRARY.md`.
- **Legacy-feature replacement surfaces (2026-07-23b):** these replace the deleted legacy copy sources; route each feature exclusively through the listed access path.
  - House glossary: `renderHouseGlossary({ house, voice })` (natal resolver) — replaces the App.tsx hardcoded glossary. Dual voice.
  - Aspect patterns: `renderAspectPattern({ type, apexTitle?, mode?, element?, activation?, voice })` (natal resolver) — types t_square | grand_square | grand_trine | kite | yod | mystic_rectangle. Replaces the astro-knowledge copy entries; the DETECTION engine stays in the app, all words come from here. `activation: true` returns the transit-trigger card.
  - Same-planet synastry: route through `renderSynastryAspect({ planetA, planetB, aspect, otherName })` with planetA === planetB; all 10 planets covered (outers are honest generational copy). Delete the legacy samePlanetSynastry key scheme and its emergency key.
  - Career vertical: 54 owner-authored cards at `authored/career-*` (career-transit, career-natal-aspect, career-placement, career-retro, career-return, career-transit-house; the verticals rule applies: employment language serves ONLY career/education surfaces). Career archetype tables are vocabulary/hook rows at `fallback-vocab/career-*` and `fallback-hook/career-*` (sign-tone, sign-essence, house-theme, planet-tenth, planet-tenth-tldr, node-mode, hemisphere) — the careerArchetype engine keeps its selection logic and reads these rows instead of its deleted embedded tables.
  - Soul Roadmap: voice-neutral phrases at `fallback-vocab/roadmap-*` (theme, sun, path, moon-style, moon-contribution, motto per sign) — the SoulRoadmapCard frame assembles them in place of its deleted signRoadmaps table.
- `renderDailyGlance({ natal, aspect } | { house })` — the daily At-a-Glance slot (Copy Batch A): engine-hidden aphorism headline + body driven by the transiting Moon per `DAILY-HOROSCOPE-ASSEMBLY-SPEC.md`. Pass the Moon's tightest applying natal aspect, or the Moon's whole-sign house when no aspect is within 5 degrees. The ONLY surface in the app with no astrology vocabulary, by design; do not strip astrology from other surfaces to match it.
- `renderSkyPlacement({ planet, sign, events })` — sky page planet-in-sign article (the page the legacy "active in a bold, fast, self-starting way... treat it as weather" helper was serving; decommission that path). Title + duration are app chrome; this returns the longer-form write-up: per-planet pace/theme paragraph in the sign's register, sign lore, the sign's trap, then one paragraph per engine-supplied aspect event (same event objects as `renderSkySeason`; pass sentence-initial dateLines like "Through Saturday"). Covers all 13 bodies x 12 signs (156/156, parity-verified). Article structure follows the owner's ingress-sample register: you-voice opener, mechanics/collective write-up, sign lore, sign trap, practice, aspect paragraphs across the duration, element collective close, blessing sign-off. Owner-authored ingress articles at `authored/sky-ingress/{planet}/{sign}` render verbatim first. The aspect list under the article ("Gifts"/"Lessons" cards) must also stop rendering glossary text ("~2 Signs / ~60°...") and use these event frames or `renderTransitLabel`.

### Status / caveats

- **Fallthrough clarifications (from the 2026-07-22 integration round):**
  - `Pluto square Chiron -> authored/transit-aspect/any/chiron/conjunction` is CORRECT, not a bug. Batch 3 sharing is heavy-aware: when a heavy body is involved, the conjunction unit doubles as the hard unit (a heavy conjunction reads as pressure), so hard aspects legitimately borrow it. The owner reviewed this exact render and approved it. Selection tests should assert this sharing, not forbid it.
  - **Do not strip files from the package folder.** "Import only dist" means the APP imports only `dist/tldr-content.js`; the package directory itself stays complete, because `tests/verify-*.mjs` import the Node reference resolvers (`resolver/*.mjs`). Deleting them breaks the verifiers. Do not edit and do not delete; the package ships as a unit.
  - **Never hardcode row counts.** The Supabase import derives counts from the package files at import time and verifies the dashboard against that computed number. Computed counts for THIS zip (always re-derive from the files at import time; do not trust any hand-written number, including this one): 1,311 authored cards, 1,555 hook rows, 480 vocabulary rows, 22 templates. (Compat-pair coverage is now complete in BOTH directions for all five planets: moon/sun via compat-deep, mercury/venus/mars/jupiter/saturn via compat-pair, 144 directed cards each.) If your local copy shows different numbers, it has diverged; re-pull the zip before importing.
- **Use the prebuilt bundle.** `dist/tldr-content.js` is the Codex-ready ESM build of both resolvers (see `dist/README.md`). Import it directly; do not edit, fork, or re-typecheck `resolver/*.ts` in the app. If something needs changing, it changes in this package.
- Review status: ALL rows in this package are APPROVED (owner sign-off through 2026-07-23 — the final approval covered the reversed compat batches for all five planets, the connection transit variant lines, and the daily At-a-Glance library). Every surface is cleared for reader release. Keep gating render eligibility on `review_status` anyway, so future draft rows stay dark until approved. The `review_status` gate keeps them dark until flipped.
- Browser/TypeScript port: `resolver/renderTransitSynastry.browser.ts`. Import it with the natal resolver file (it shares `SourceGapError` and types from `renderFallback.browser.ts`): `createTransitSynastryRenderer(transitLibJson, templatesJson, rowsJson)`. Verified output-identical to the Node reference across 4,102 renders (all aspect/house/label/return/compat/synastry combos).
- Decommission the legacy transit/synastry fallback paths per `DECOMMISSION-OLD-FALLBACKS.md` (section: transit + synastry import). Selection is authored-or-v3-or-SOURCE_GAP, never authored-or-old-helper.

---

## 2026-07-28: transit-aspect library v2 (owner-approved)

The full slow-planet aspect library is now authored: transiting Saturn, Chiron, Jupiter,
Neptune, Uranus, Pluto x each natal target (soft/hard, incl. Midheaven/Ascendant), Lilith
(conjunction/opposition only - Walker canon, renderer-guarded), and the Nodes
(conjunction cards for 11 transiting planets per node). Keys:
`authored/transit-aspect/{transiting}/{natal}/{soft|hard|conjunction}`.

- Closers carry `{{aspectWord}}` and `{{untilDate}}` slots; `renderTransitAspect` fills
  them from the live aspect + `window` ("Until <Month> <day>"). No window -> the until
  clause drops cleanly.
- Neptune pressure cards append a rotating fog-decision note
  (`fallback-hook/fog-note/variant-1..4`, reader-only by design).
- NEW surfaces needing App call sites:
  - `authored/transit-return/north-node` via `renderTransitReturn({ planet: "north-node" })`
    - show in the year the transiting node conjoins the natal North Node (~ages 19/37/56/75).
  - `authored/point-explainer/north-node|south-node` - static explainers for the Nodes
    (render near node cards or in the placement glossary).
- ENGINE-SIDE gating still to do: Lilith contacts should only FIRE at conjunction/opposition
  within a 3-degree orb (renderer already SOURCE_GAPs other aspects; tightening the orb is
  an engine change). Node cards fire on conjunction only.
- Saturn house intros (1-12) + Aries sign layers replaced with the owner's 2026-07-28
  rewrite (body_you only; body_they retains the prior approved friend-voice set pending an
  owner-reviewed friend conversion).
