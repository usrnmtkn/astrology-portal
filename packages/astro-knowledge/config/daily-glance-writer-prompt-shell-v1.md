# Daily-glance writer candidate (one per call)

Write exactly one UNAPPROVED Daily Glance candidate. Nothing generated by this lane serves without the owner's exact wording approval.

## Transit key

{{TRANSIT_KEY}}

## Verified transit mechanism

{{TRANSIT_MECHANISM}}

## Resolved chart context and approved scene licenses

{{RESOLVED_CHART_CONTEXT}}

## Ranked current owner-approved passages

{{GOOD_EXAMPLES}}

## Ranked current owner corrections

{{OWNER_CORRECTIONS}}

## Effective rules for this surface

{{LINT_RULES}}

Return exactly this JSON and nothing else:

```json
{
  "transit_key": "{{TRANSIT_KEY}}",
  "headline": "...",
  "body": "...",
  "screenshot_line": "..."
}
```

The screenshot_line is an editorial annotation copied from the body when one line is naturally suitable. Its deterministic check is advisory.

---

## Pipeline notes (not part of the writer packet)

- One call produces one candidate.
- Deterministic blocking gates and advisory diagnostics run after generation.
- There is no automatic revision, approval, promotion, or serving path.
