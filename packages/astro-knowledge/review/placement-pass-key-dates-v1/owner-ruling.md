# Placement pass Key dates: owner ruling

Date: 2026-08-10

Owner ruling, verbatim:

> New job: placement-pass-key-dates (ALL sky placements, not only Lilith). On
> every sky placement page, populate the existing Key dates section with
> engine-computed facts for that body's current residency in the page's sign:
> 1. Every residency pass, labeled with the already-approved pass strings
> ("Pass {n} of {count}") and date ranges, year-aware per the date-rendering
> rule. Single-pass residencies show the one entry/exit range without a pass
> label.
> 2. Station events (retrograde and direct) falling within the residency, using
> the existing station title pattern ("{Body} stations retrograde/direct").
> Bodies that never station (Sun, Moon) show passes/range only.
> Header window continues to show the current pass; body {{exitDate}} continues
> to mean final exit; no core copy changes anywhere. Reader-facing strings
> composed ONLY from already-approved labels plus engine dates; any new prose
> needed is a stop-and-report for owner wording. Fail closed per body: render
> only what the engine can verify, never guess. Regressions: Lilith/Capricorn
> multi-pass fixture (2026 first pass, 2027 final exit, correct years); a
> Mercury residency fixture showing station dates; Sun page unchanged except
> the range; a body with no station in residency shows no station line.
> Flight rule v2; report verification and stop at the review wall.

Approved label provenance is preserved in
`packages/astro-knowledge/review/transit-pass-labels-approval-2026-08-08.md`.
No content-row body is changed by this package.

## Addendum: true-Lilith explanatory line

Owner-approved 2026-08-10. On true-Lilith placement pages only, render this exact line once above the Key dates pass list:

> True Black Moon Lilith stations about once a month, so it crosses the same degrees several times before it finally moves on.

No placeholders and no other bodies. The line is byte-protected by regression.
