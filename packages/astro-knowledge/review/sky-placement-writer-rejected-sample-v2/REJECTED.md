# Rejected writer packet

This packet is preserved only to audit the failed retrieval. It must not be sent to a writer model.

The six passages are insufficiently matched to a complete Jupiter-in-Libra Sky Placement article, the task incorrectly labels a full-article request as `turn`, and the verified astrology block does not support a concrete central scenario.

The former `model-input.md` is renamed `rejected-model-input-audit.md` so it cannot be mistaken for an approved first-call input.
