# Sky Placement Mustache fallback cutover

Date: 2026-08-23

## Owner approval

> I approve moving the 12 Lilith pages from the old unframed four-slot renderer into the canonical frame using the same approved copy, then deleting the two obsolete renderer paths. No wording changes are involved.

## Verified scope

- No approved source-row wording changed.
- Standalone sign paragraphs are inputs to the canonical frame, never finished pages.
- The 12 approved Lilith four-slot bodies render inside `sky-placement-frame-v3` with final-exit substitution preserved.
- The intentional Moon entry renderer remains unchanged.
- Package coverage: 168 pages, 0 blanks, 0 unresolved Mustache variables.
- Selected policies: 55 `sky-placement-continuous-v2`, 101 `sky-placement-frame-v3`, 12 `sky-placement-moon-entry-v1`.
