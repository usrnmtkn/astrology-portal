[
{"path":"scripts/test-sky-placement-studio-api.mts","before":" let row = stored.find(item => item.content_key === key && item.status === 'LIVE');\n const priorPublished = JSON.stringify(row.sections.packageRecord);","after":" const live = stored.find(item => item.content_key === key && item.status === 'LIVE');\n const priorPublished = JSON.stringify(live.sections.packageRecord);\n // Earlier refusal cases intentionally leave a proposal open. Resume it, just\n // as the editor does; creating a second revision must remain a conflict.\n let row = stored.find(item => item.status === 'DRAFT' && item.source_snapshot?.targetRowId === live.id) ?? live;"},
{"path":"scripts/test-sky-placement-studio-api.mts","before":" const copy = { ...row.sections.packageRecord, ingress: composition, placementArticle: template, placementArticleDirect: '', placementArticleRetrograde: '' };","after":" const copy = { ...(row.sections.packageDraft ?? row.sections.packageRecord), ingress: composition, placementArticle: template, placementArticleDirect: '', placementArticleRetrograde: '' };"},
{"path":"scripts/test-sky-placement-studio-api.mts","before":" assert.equal(JSON.stringify(row.sections.packageRecord), priorPublished, 'Incomplete draft cannot alter the published baseline');","after":" assert.equal(JSON.stringify(stored.find(item => item.id === live.id).sections.packageRecord), priorPublished, 'Incomplete draft cannot alter the published baseline');"},
{"path":"scripts/test-placement-dignity-editor.mts","before":"import './apps/admin/src/studio-system.css';","after":"import './apps/web/src/styles/startup.css';\nimport './apps/admin/src/studio-system.css';"},
{"path":"scripts/test-placement-dignity-editor.mts","before":"      const selection = page.getByTestId('placement-dignity-selection');","after":"      await page.evaluate(() => document.fonts.ready);\n      const selection = page.getByTestId('placement-dignity-selection');"},
{"path":"apps/web/src/content/studioCustomVariables.d.mts","before":"export function resolveStudioVariableCopy(copy: unknown, bindings?: StudioVariableDefinition[], context?: Record<string, any>): string;","after":"export function resolveStudioVariableCopy(copy: unknown, bindings?: StudioVariableDefinition[], context?: Record<string, any>, deferredNames?: readonly string[]): string;"},
{"path":"apps/web/src/content/studioCustomVariables.d.mts","before":"export function resolveStudioVariableRecord<T extends Record<string, any>>(record: T, context?: Record<string, any>): T;","after":"export function resolveStudioVariableRecord<T extends Record<string, any>>(record: T, context?: Record<string, any>, deferredNames?: readonly string[]): T;"}
]
