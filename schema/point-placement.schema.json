{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://astro-knowledge.local/schema/point-placement.schema.json",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "kind", "point", "tldr", "body", "business", "shadow", "policy", "voiceNeutral", "status"],
  "properties": {
    "id": { "type": "string" },
    "kind": { "enum": ["sign", "house"] },
    "point": { "type": "string" },
    "sign": { "type": "string" },
    "house": { "type": "integer", "minimum": 1, "maximum": 12 },
    "tldr": { "type": "string" },
    "body": { "type": "string" },
    "business": { "type": "string" },
    "shadow": { "type": "string" },
    "policy": { "type": "string" },
    "note": { "type": "string" },
    "voiceNeutral": { "const": true },
    "status": { "enum": ["TODO", "DRAFT", "REVIEWED", "LIVE"] }
  }
}
