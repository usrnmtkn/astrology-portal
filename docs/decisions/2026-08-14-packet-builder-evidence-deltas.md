# Packet-builder retirement evidence deltas

Status: deterministic migration record. No provider calls, serving changes, or approval changes.

This record compares each private packet builder with the governed resolver on the same target before its call sites switch. Surface format, routing, lint, scene, and voice strategy remain surface-owned; astrology evidence now comes from `knowledge-resolver.js`.

## Sky Placement: Jupiter in Libra

Comparison target: `placement-sign/jupiter/libra`, surface `sky`, register `collective`.

The legacy astrology packet contained eight distinct strings. The governed packet contains eleven distinct strings after the Sky output-ban evidence filter. Four strings are byte-identical across both packets.

Meaning retained in the governed packet:

- the reviewed Jupiter-in-Libra combined placement meaning;
- the reviewed collective-gift boundary;
- the reviewed Jupiter overview;
- the reviewed Jupiter cycle;
- supported domains, unsupported-domain warnings, challenge, and scenario policy, now as individually addressed records rather than derived packet fields.

Legacy strings no longer supplied in their old representation:

- the curly-apostrophe planetary-file sign sentence beginning `If Jupiter is in Libra...`; an ASCII equivalent remains as explicitly `unverified` mechanism-reference evidence, while the reviewed placement boundary remains primary;
- `indecision` and `over-accommodating` as two derived fragments; the reviewed source value `indecision and over-accommodating.` remains intact;
- the concatenated planetary source-passage string; its Jupiter overview and cycle remain as separate governed records, while the sign sentence follows the disposition above.

Added governed records:

- individually addressable scenario policy, supported domains, unsupported-domain warnings, TLDR, and combined challenge from the reviewed placement source;
- the hash-pinned authored-placement mechanism reference, labelled `unverified` rather than voice evidence;
- the V13 Jupiter mechanism record, with authority, temporality, source hash, and evidence hash.

The resolver initially found additional owner-prose records that violate Sky's packet-source output bans. The named filter `sky-placement-output-ban-safe-evidence-v1` excludes them before packet hashing. The packet records the filter ID and filtered-record count. No excluded record becomes prompt evidence.

Unchanged surface-owned material:

- six selected owner passages;
- four exact format exemplars;
- the owner-approved Jupiter-in-Libra reference article;
- the owner-selected Mars-in-Aries writing benchmark;
- warmth evidence, vocabulary policy, structural slots, output bans, routing, and the continuous-article contract.

## Daily Glance: `square/mars` and all 31 configured keys

Comparison target: `daily/square/mars`, surface `daily`, register `daily`.

The legacy packet supplied two verified fact strings. The governed packet supplies the same two strings byte-for-byte, in the same configured order, with canonical target ID, authority class, source reference, index hash, source hash, evidence hash, and packet hash. No astrology string is dropped or added.

The same deterministic comparison runs across all 31 Daily keys from the pilot and batches 1-3. Every configured fact string must appear byte-identically in its resolver packet. The config remains the Daily-specific fact boundary; indexing it does not approve generated copy.

Unchanged surface-owned material:

- owner passages and matching-exemplar policy;
- warmth and scene evidence;
- phrase-library headline options;
- output schema, owner guidance, Daily lint rules, batch rules, routing, and cost accounting.

## Result

The two named private `buildPacket` implementations are retired. Their files may still provide surface adapters and lint helpers, but governed astrology evidence is resolved and hash-checked centrally. Any later evidence change is visible through packet hashes and the migration tests rather than occurring silently.
