# Supplied daily Sky summary bundle

The reader and Studio now share all 24 supplied Sun/Moon summary clauses. The 22 newly connected sentences add copy to the shipped catalog. Detailed provenance was moved into docs/content-review/sky-summary-supplied-copy-2026-09-08.json so it does not travel with the application.

A fresh web build using the release CI placeholder environment measures 2,934,994 bytes of level-9 gzip JavaScript. The prior aggregate limit was 2,934,000 bytes. The new limit is 2,936,000 bytes, a 2,000-byte increase for the requested copy. Reader startup, individual chunks, and CSS limits are unchanged. No prose was shortened to meet the budget.

The Studio entry measures 617.6 kB raw, 175.2 kB gzip, and 307.9 kB total gzip. Its raw entry/largest-chunk limits increase from 616,000 to 618,500 bytes for the same shared copy. Both admin gzip limits remain unchanged.
