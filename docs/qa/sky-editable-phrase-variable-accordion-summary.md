The Content Studio Sky placement variable reference now exposes a dedicated **Editable phrase variables** accordion immediately below **Calculated Sky variables**. It uses the same `SKY_WRITING_LIBRARY_GROUPS` registry as the Writing Library editor and lists exact source tokens, labels, descriptions, and scopes so editors can find phrase variables without searching Advanced source tools.
