# Source-grounded reader rendering diagnosis

Scope: focused rendering failure only. Calculation/API, Dashboard IA, source-gap cleanup, and unrelated reader surfaces remain out of scope.

## Natal placement: Sun in Aquarius in the 9th house

- Canonical route: `#/you/placement/sun-aquarius-9h`
- Calculated fact object used by the reader:
  - `planet`: `Sun`
  - `sign`: `Aquarius`
  - `house`: `9`
  - `degree`: `29°25'`
  - `dignity`: `Constrained · Detriment`
  - `ruler`: `Saturn`
  - `ruler placement`: `Saturn in Virgo in the 4th house`
- Bad pre-fix resolver path:
  - Component: `natalPlacementDetailArticle()` in `apps/web/src/App.tsx`
  - Branch: `natalPlacementModularSections()`
  - Modules reached: `natalPlacementSignModule()`, `natalPlacementHouseModule()`, `natalPlacementRulerModule()`
  - Fallback source: generated content or `emergencyNatalPlacementCopy()`
  - Final component fields: `YouTransitArticle.sections[]`
- Bad rendered language source:
  - Generic sign module rendered dictionary-style planet/sign definitions.
  - Generic house module rendered a reusable house definition, not a sign-house synthesis.
  - The route bypassed the package’s layered `planetInSignStory`, `signHouseSynthesis`, modifier, ruler, and focal-pattern fields.
- Materializer issue:
  - `scripts/materialize-final-source-grounded-package.mjs` produced dashboard-shaped rows, but early clause construction converted raw source fragments into generic clauses such as `core_behavior` and `house_synthesis`.
  - Those rows existed, but they were not the executable reader composition model.
- Current normalized record ID:
  - `dashboard.natal-placement.sun.aquarius.house_9`
- Current template:
  - `natal-placement-layered-v1`
  - version: `2026-07-13-source-grounded`
- Current runtime branch:
  - Initial render: `sourceGroundedNatalPlacementSections()` from local source-grounded model.
  - Hydrated render: same branch unless an eligible non-local authored/live row exists.
  - Provenance parity: `source-grounded-local-template / source-grounded-local-template`
- Current rendered slots:
  - `planetInSignStory`
  - `signHouseSynthesis`
  - `dignityModifier`
  - `rulerBridge`
  - `supportivePatterns`
  - `challengingPatterns`
- Current source keys by slot are emitted in:
  - `scripts/generated/source-grounded-reader-fixtures.md`
  - `scripts/generated/source-grounded-reader-fixtures.json`

## Sky retrograde: Mercury retrograde in Cancer on July 12, 2026

- Canonical route: `#/sky/placement/mercury/cancer`
- Calculated fact object used by the reader:
  - `planet`: `Mercury`
  - `sign`: `Cancer`
  - `motion`: `retrograde`
  - `phase`: `retrograde passage`
  - `current date`: `July 12, 2026`
  - `active window`: `June 29, 2026 - July 23, 2026`
- Bad pre-fix resolver path:
  - Component: `currentSkyPlacementDetailArticle()` in `apps/web/src/App.tsx`
  - Branch: `liveGeneratedContentByKeys()` with `skyPlacementGeneratedContentKeys()`
  - Snapshot record selected: `sky.placement.mercury.cancer`
  - Template family selected: current-sky planet-in-sign
  - Final component fields: `SkyDetail.body`
- Bad rendered language source:
  - `apps/web/src/content/skyContentSnapshot.json` row `sky.placement.mercury.cancer`
  - Generated by `scripts/generate-normalized-sky-snapshot.mjs`
  - It used mechanical slot assembly: planet action plus sign behavior plus generic instruction.
  - It did not select the retrograde clause contract.
- Current normalized record ID:
  - `sky.retrograde.mercury.cancer.retrograde-passage`
- Current template:
  - `sky-retrograde-clause-contract-v1`
  - version: `2026-07-13-source-grounded`
- Current runtime branch:
  - Initial render: `sourceGroundedSkyPlacementParagraphs()` detects `position.motion === "retrograde"` and calls `composeSkyRetrograde()`.
  - Hydrated render: same branch for retrograde placement body.
  - Provenance parity: `source-grounded-local-template / source-grounded-local-template`
- Current rendered slots:
  - `recognizableLivedSituation`
  - `retrogradeBehavior`
  - `returningMatter`
  - `practicalResponse`
  - `timingPhase`
- Current source keys by slot are emitted in:
  - `scripts/generated/source-grounded-reader-fixtures.md`
  - `scripts/generated/source-grounded-reader-fixtures.json`

## Bad-language origin summary

- Materializer: produced dashboard-shaped records, but some generated clauses still used raw source fragments or generic clause construction.
- Template selection: Mercury retrograde placement route selected the generic planet-in-sign row before the retrograde clause contract.
- Fallback precedence: local normalized snapshot rows could win before the package-composed runtime fallback.
- Reader bypass: natal placement route used legacy modular sign/house/ruler modules instead of the package’s layered composition.
- Stale snapshot data: broad `skyContentSnapshot.json` still contains old language on many unrelated rows. The focused Mercury retrograde body no longer depends on those rows, but broad cleanup is not included in this checkpoint.

## Proof command

```sh
node --experimental-strip-types scripts/test-source-grounded-reader-fixtures.mjs
```

This test renders the final strings for:

- Sun in Aquarius in the 9th house
- Mercury retrograde in Cancer on July 12, 2026
- one natal placement without reliable birth time
- one focal natal aspect from both focal directions
- one short-term personalized transit
- one long-term personalized transit

It rejects the generic constructions named in the prompt and verifies source-key resolution for every rendered slot.
