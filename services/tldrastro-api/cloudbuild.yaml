substitutions:
  _REGION: us-central1
  _REPOSITORY: tldrastro
  _SERVICE: tldrastro-api
  _ALLOWED_ORIGINS: https://tldrastro.vercel.app,https://www.tldrastro.com,https://tldrastro.com
  _EPHEMERIS_BUCKET: tldrastro-swisseph
  _EPHEMERIS_PATH: /opt/swisseph
  _MIN_INSTANCES: "1"
  _MAX_INSTANCES: "20"
  _CONCURRENCY: "20"
  _CPU: "1"
  _MEMORY: 1Gi
  _GOOGLE_TIMEZONE_SECRET: google-maps-timezone-api-key

steps:
  - id: build-api-image
    name: gcr.io/cloud-builders/docker
    args:
      - build
      - -f
      - services/tldrastro-api/Dockerfile
      - -t
      - ${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPOSITORY}/${_SERVICE}:$BUILD_ID
      - services/tldrastro-api

  - id: push-api-image
    name: gcr.io/cloud-builders/docker
    args:
      - push
      - ${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPOSITORY}/${_SERVICE}:$BUILD_ID

  - id: deploy-cloud-run
    name: gcr.io/google.com/cloudsdktool/cloud-sdk:slim
    entrypoint: gcloud
    args:
      - run
      - deploy
      - ${_SERVICE}
      - --image=${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPOSITORY}/${_SERVICE}:$BUILD_ID
      - --region=${_REGION}
      - --platform=managed
      - --allow-unauthenticated
      - --execution-environment=gen2
      - --port=8000
      - --cpu=${_CPU}
      - --memory=${_MEMORY}
      - --concurrency=${_CONCURRENCY}
      - --min-instances=${_MIN_INSTANCES}
      - --max-instances=${_MAX_INSTANCES}
      - --set-env-vars=^::^TLDR_ASTRO_ALLOWED_ORIGINS=${_ALLOWED_ORIGINS}::TLDR_ASTRO_EPHEMERIS_PATH=${_EPHEMERIS_PATH}
      - --set-secrets=GOOGLE_MAPS_TIMEZONE_API_KEY=${_GOOGLE_TIMEZONE_SECRET}:latest
      - --add-volume=name=swisseph-data,type=cloud-storage,bucket=${_EPHEMERIS_BUCKET},readonly=true
      - --add-volume-mount=volume=swisseph-data,mount-path=${_EPHEMERIS_PATH}
      - --startup-probe=httpGet.path=/ready,httpGet.port=8000,initialDelaySeconds=0,failureThreshold=3,timeoutSeconds=10,periodSeconds=10

images:
  - ${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPOSITORY}/${_SERVICE}:$BUILD_ID
